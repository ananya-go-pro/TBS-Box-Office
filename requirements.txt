# Core Django stack
Django==5.1
sqlparse==0.5.1
tzdata==2024.1
asgiref==3.8.1

# Frontend / Styling
django-tailwind==3.8.0

# Email (optional: if Gmail API integration used for ticket emails)
EZGmail==2024.8.13
google-api-python-client==2.142.0
google-auth==2.34.0
google-auth-oauthlib==1.2.1
google-auth-httplib2==0.2.0

# QR & Image utilities (optional: if tickets / passes use QR codes)
qrcode==7.4.2
PyQRCode==1.2.1
pypng==0.20220715.0
pillow==10.4.0

# HTML parsing (if scraping / dynamic templates need it)
beautifulsoup4==4.13.5
soupsieve==2.8

# Optional encryption or security-related libraries (if reused from your other repos)
pycryptodome==3.22.0
stegano==0.11.5

# Date & timezone helpers
pytz==2024.1
DateTime==5.5
